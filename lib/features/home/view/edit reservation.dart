import 'package:flutter/cupertino.dart';

class EditReservationDetails extends StatelessWidget {
  const EditReservationDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
